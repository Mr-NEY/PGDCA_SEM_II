#include<iostream.h>
#include<conio.h>
void main(){

	clrscr();

	int arr[] = {2, 4, 6, 8, 10} ;
	int  size = sizeof(arr) / sizeof(arr[0]);


	for(int i = 0 ;i < size; i ++){
	    int n = arr[i];
	    cout<<n;
	    for(int j = 0; j < n ; j++){
		cout<<"*";
	    }
		cout<<"\n";
	}
	getch() ;
}